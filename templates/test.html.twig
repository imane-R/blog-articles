{% extends 'layout.html.twig' %} {# on hérite de la page base.html.twig tout (lick style , javascript .... #}

{% block content %}
  <div class="row mt-5 justify-content-center text-center">
    <h1>Dérnier article publié</h1>
    <div>
      <h3>{{ article.titre }}</h3>
      <h6 class="text-muted">publié le :{{ article.dateDeCreation }}</h6>
      <h6>auteur : {{ article.auteur }}</h6>
      <p>{{ article.contenu }}</p>
    </div>
  </div>
{% endblock %}
